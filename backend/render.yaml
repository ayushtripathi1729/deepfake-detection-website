services:
  - type: web
    name: my-deepfake-backend
    env: python
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: |
      gunicorn app:app
    plan: starter
    envVars:
      - key: PYTHONPATH
        value: .
